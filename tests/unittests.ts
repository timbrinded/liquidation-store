// TODO: Add variety of unit tests

describe("When I start the server", () => {
    it("is able to listen to the websocket", async () =>{
        //write test
    })

    it("closes gracefully", async () =>{
        //write test
    })

    it("can support multiple connections", async () =>{
        //write test
    })
})